// import db from db


// eslint-disable-next-line no-unused-vars
export async function get({params}) {
  //db.collection.find(params)
  // data
  return {
    status: 200,
    body: {
      data: 'test'
    }
  }
}